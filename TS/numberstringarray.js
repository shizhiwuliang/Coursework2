var age = 30; // 整数
var score = 75.5; // 浮点数
var name = "Alice"; // 使用双引号
var greeting = 'Hello, ' + name; // 使用单引号
var welcomeMessage = "Welcome, ".concat(name); // 使用模板字符串
var list = [1, 2, 3]; // 方括号表示法
var fruits = ['Apple', 'Orange', 'Banana']; // 泛型数组表示法
var map = new Map();
map.set('apples', 5);
map.set('bananas', 10);
// 获取值
console.log(map.get('apples')); // 输出 5
var person = ['Alice', 28]; // 第一个元素是string，第二个是number
